<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.aooms.mybatis.mapper.UserMapper">

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        test_id AS testId, name, age, test_type AS testType, role, phone
    </sql>

    <delete id="deleteAll">
        DELETE FROM USER
    </delete>

    <select id="selectListByWrapper" resultType="net.aooms.mybatis.entity.User">
        SELECT * FROM USER
        <!-- 判断 wrapper 是否为空 emptyOfWhere -->
        <where>
            ${ew.sqlSegment}
        </where>
    </select>

    <select id="selectListBySQL" resultType="map">
        SELECT * FROM USER
    </select>

    <select id="selectMap" resultType="java.util.Map">
        SELECT * FROM USER WHERE  name li
    </select>

</mapper>